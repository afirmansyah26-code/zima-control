{
  "name": "zima-control-center",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace=@zima-control-center/application-registry-contracts && npm run build --workspace=@zima-control-center/core && npm run build --workspace=@zima-control-center/zimaos-adapter && npm run build --workspace=@zima-control-center/docker-adapter",
    "build": "npm run build:packages && npm run build --workspace=@zima-control-center/api && npm run build --workspace=@zima-control-center/worker && npm run build --workspace=@zima-control-center/web",
    "pretypecheck": "npm run build:packages",
    "typecheck": "npm run typecheck --workspaces",
    "pretest": "npm run build",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prisma": "^6.19.0",
    "tsx": "^4.20.6",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0"
  }
}
